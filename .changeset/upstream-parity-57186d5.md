---
"any-llm-ts": minor
---

Sync with Python any-llm through `57186d5`: add Gemini text Interactions Responses (including streaming), expose `messagesNative` for Anthropic Messages endpoints, and treat Cascadia/llama.cpp API keys as optional.
