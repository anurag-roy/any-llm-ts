---
"any-llm-ts": patch
---

Sync with Python any-llm through `e822b28`: Mistral streaming now recovers answers wrapped in `<response>` tags, and Anthropic/Gemini content-filter stops populate a typed `refusal` field.
