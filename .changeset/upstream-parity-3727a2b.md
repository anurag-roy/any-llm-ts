---
"any-llm-ts": patch
---

Sync with Python any-llm through `3727a2b`: emit Anthropic usage on a trailing empty-choices stream chunk, accept Gemini `input_audio` formats beyond MP3/WAV, and read HTTP status off attached SDK response objects.
