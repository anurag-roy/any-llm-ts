---
"any-llm-ts": minor
---

Add validated self-describing Provider descriptors and gateway-safe completion controls. OpenAI and Anthropic now accept per-operation abort signals, can disable SDK retries, expose the Provider SDK dispatch boundary, and prevent Provider extensions from overriding normalized completion fields.
